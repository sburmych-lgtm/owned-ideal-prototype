# Skill: Visual QA

Before declaring UI done:

1. Open live or local URL in Playwright.
2. Capture 390 and 1440 viewports (hero, results, booking).
3. Check: logo not clipped; sticky not covering H2; contrast; reduced-motion.
4. Compare against `docs/00` + `docs/03`.
5. Save PNGs under `docs/screenshots/` with names `proto-mobile-*.png` / `proto-desktop-*.png`.

Code compile ≠ done. Screenshots required.
